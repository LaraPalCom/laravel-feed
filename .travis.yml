language: php

php:
  - 7.1.3
  - 7.1
  - 7.2

sudo: false

before_script:
  - composer self-update
  - composer install

script:
  - vendor/bin/php-cs-fixer fix --no-interaction --dry-run --diff -v
  - vendor/bin/phpunit
